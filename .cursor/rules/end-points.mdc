---
alwaysApply: true
---

직원, 부서, 알림

```typescript
/**
 * CMS 통합 서비스 인터페이스
 *
 * @description
 * - 직원, 부서, 직책, 직급, 조직 하이라키에 대한 조회 메서드와 알림 전송 메서드를 정의합니다.
 * - 백엔드 API와의 계약을 추상화하여 Mock 서비스와 실 API 서비스의 교체를 용이하게 합니다.
 * - 모든 CMS 섹션에서 공통으로 사용할 수 있는 통합 서비스 인터페이스입니다.
 */

import type { ApiResponse } from '../_types/common.model';
import type {
  EmployeeDto,
  DepartmentDto,
  PositionDto,
  RankDto,
} from '../_entities/employee.dto';
import type { OrganizationHierarchyDto } from '../_entities/organization.dto';
import type {
  NotificationRequestDto,
  NotificationResponseDto,
} from '../_entities/notification.dto';

/**
 * CMS 통합 서비스 인터페이스
 *
 * @description
 * - 직원, 부서, 직책, 직급, 조직 하이라키: 조회 메서드만 제공
 * - 알림: 전송 메서드만 제공
 */
export interface CmsService {
  // ========== 직원 조회 메서드 ==========
  /**
   * 직원 목록 조회
   *
   * 직책, 직급, 부서 정보가 포함된 DTO를 반환합니다.
   */
  직원_목록을_조회_한다: () => Promise<ApiResponse<EmployeeDto[]>>;

  /**
   * 직원 상세 조회
   *
   * @param employeeId - 조회할 직원 ID
   *   - 직책, 직급, 부서 정보가 포함된 DTO를 반환합니다.
   */
  직원을_조회_한다: (employeeId: string) => Promise<ApiResponse<EmployeeDto>>;

  // ========== 부서 조회 메서드 ==========
  /**
   * 부서 목록 조회
   *
   * 상위 부서 정보가 포함된 DTO를 반환합니다.
   */
  부서_목록을_조회_한다: () => Promise<ApiResponse<DepartmentDto[]>>;

  /**
   * 부서 상세 조회
   *
   * @param departmentId - 조회할 부서 ID
   *   - 상위 부서, 소속 직원, 자식 부서 정보가 포함된 DTO를 반환합니다.
   */
  부서를_조회_한다: (
    departmentId: string,
  ) => Promise<ApiResponse<DepartmentDto>>;

  // ========== 직책 조회 메서드 ==========
  /**
   * 직책 목록 조회
   */
  직책_목록을_조회_한다: () => Promise<ApiResponse<PositionDto[]>>;

  /**
   * 직책 상세 조회
   *
   * @param positionId - 조회할 직책 ID
   *   - 해당 직책을 가진 직원 목록이 포함된 DTO를 반환합니다.
   */
  직책을_조회_한다: (positionId: string) => Promise<ApiResponse<PositionDto>>;

  // ========== 직급 조회 메서드 ==========
  /**
   * 직급 목록 조회
   */
  직급_목록을_조회_한다: () => Promise<ApiResponse<RankDto[]>>;

  /**
   * 직급 상세 조회
   *
   * @param rankId - 조회할 직급 ID
   *   - 해당 직급을 가진 직원 목록이 포함된 DTO를 반환합니다.
   */
  직급을_조회_한다: (rankId: string) => Promise<ApiResponse<RankDto>>;

  // ========== 조직 하이라키 조회 메서드 ==========
  /**
   * 조직 하이라키 조회
   *
   * 전체 조직 계층 구조를 조회합니다.
   * 각 부서의 직원 정보가 포함된 DTO를 반환합니다.
   *
   * @param options - 조회 옵션
   *   - includeEmployees: 직원 정보 포함 여부 (기본값: true)
   */
  조직_하이라키를_조회_한다: (options?: {
    includeEmployees?: boolean;
  }) => Promise<ApiResponse<OrganizationHierarchyDto>>;

  // ========== 알림 전송 메서드 ==========
  /**
   * 알림 전송
   *
   * @param entityId - 문서/콘텐츠 ID (각 섹션의 문서 ID)
   * @param request - 알림 전송 요청 데이터
   */
  알림을_전송_한다: (
    entityId: string,
    request: NotificationRequestDto,
  ) => Promise<ApiResponse<NotificationResponseDto>>;
}
```

문서

```typescript
/**
 * Homepage CMS 공통 서비스 인터페이스
 *
 * @description
 * - Homepage 문서, 카테고리, 번역, 첨부파일, 언어에 대한 통합 CRUD 명세를 정의합니다.
 * - 백엔드 API와의 계약을 추상화하여 Mock 서비스와 실 API 서비스의 교체를 용이하게 합니다.
 */

import { DocumentCode } from '@/cms/admin/homepage/_entities/homepage.entity';
import { ApiResponse } from '@/app/(planning)/plan/cms/_types/common.model';
import type {
  HomepageDto,
  HomepageCategoryDto,
  HomepageTranslationDto,
  HomepageAttachmentDto,
  CreateHomepageDto,
  UpdateHomepageDto,
  CreateHomepageCategoryDto,
  UpdateHomepageCategoryDto,
  CreateHomepageTranslationDto,
  UpdateHomepageTranslationDto,
  CreateHomepageAttachmentDto,
  UpdateHomepageAttachmentDto,
  CreateHomepageLanguageDto,
  UpdateHomepageLanguageDto,
  HomepageLanguageDto,
} from '../_entities/homepage.dto';

/**
 * Homepage CMS 통합 관리 서비스 명세
 */
export interface HomepageService {
  // ========== 문서 CRUD 메서드 ==========
  /**
   * 문서 목록 조회
   *
   * @param code - CMS 문서 타입 코드 (필수)
   *   - 해당 code로 필터링하여 문서 목록을 조회합니다.
   *   - 카테고리 정보가 포함된 DTO를 반환합니다.
   */
  문서_목록을_조회_한다: (
    code: DocumentCode,
  ) => Promise<ApiResponse<HomepageDto[]>>;

  /**
   * 문서 상세 조회
   *
   * @param documentId - 조회할 문서 ID
   *   - 카테고리 및 번역 정보가 포함된 DTO를 반환합니다.
   */
  문서를_조회_한다: (documentId: string) => Promise<ApiResponse<HomepageDto>>;

  /**
   * 문서 생성
   *
   * @param data - 생성할 문서 데이터
   *   - categoryId를 포함할 수 있으며, 직접 외래키 관계로 카테고리를 연결합니다.
   */
  문서를_생성_한다: (
    data: CreateHomepageDto,
  ) => Promise<ApiResponse<HomepageDto>>;

  /**
   * 문서 수정
   *
   * @param documentId - 수정할 문서 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   *   - categoryId를 포함할 수 있으며, null로 설정 시 카테고리 관계를 제거합니다.
   */
  문서를_수정_한다: (
    documentId: string,
    data: UpdateHomepageDto,
  ) => Promise<ApiResponse<HomepageDto>>;

  /**
   * 문서 삭제
   *
   * 문서를 삭제하면 메타데이터와 관련된 모든 언어별 문서도 함께 삭제됩니다.
   *
   * @param documentId - 삭제할 문서 ID
   */
  문서를_삭제_한다: (documentId: string) => Promise<ApiResponse<void>>;

  // ========== 카테고리 CRUD 메서드 ==========
  /**
   * 카테고리 목록 조회
   *
   * @param code - CMS 문서 타입 코드 (필수)
   *   - 해당 code로 필터링하여 카테고리 목록을 조회합니다.
   */
  카테고리_목록을_조회_한다: (
    code: DocumentCode,
  ) => Promise<ApiResponse<HomepageCategoryDto[]>>;

  /**
   * 카테고리 생성
   *
   * @param data - 생성할 카테고리 데이터
   */
  카테고리를_생성_한다: (
    data: CreateHomepageCategoryDto,
  ) => Promise<ApiResponse<HomepageCategoryDto>>;

  /**
   * 카테고리 수정
   *
   * @param categoryId - 수정할 카테고리 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  카테고리를_수정_한다: (
    categoryId: string,
    data: UpdateHomepageCategoryDto,
  ) => Promise<ApiResponse<HomepageCategoryDto>>;

  /**
   * 카테고리 삭제
   *
   * @param categoryId - 삭제할 카테고리 ID
   */
  카테고리를_삭제_한다: (categoryId: string) => Promise<ApiResponse<void>>;

  // ========== 번역 CRUD 메서드 ==========
  /**
   * 번역 목록 조회
   *
   * 특정 문서의 모든 번역을 조회합니다.
   *
   * @param documentId - 문서 ID
   */
  번역_목록을_조회_한다: (
    documentId: string,
  ) => Promise<ApiResponse<HomepageTranslationDto[]>>;

  /**
   * 번역 조회
   *
   * 특정 문서의 특정 언어 번역을 조회합니다.
   * 언어 및 첨부파일 정보가 포함된 DTO를 반환합니다.
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  번역을_조회_한다: (
    documentId: string,
    languageId: string,
  ) => Promise<ApiResponse<HomepageTranslationDto>>;

  /**
   * 번역 생성
   *
   * @param data - 생성할 번역 데이터
   */
  번역을_생성_한다: (
    data: CreateHomepageTranslationDto,
  ) => Promise<ApiResponse<HomepageTranslationDto>>;

  /**
   * 번역 수정
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  번역을_수정_한다: (
    documentId: string,
    languageId: string,
    data: UpdateHomepageTranslationDto,
  ) => Promise<ApiResponse<HomepageTranslationDto>>;

  /**
   * 번역 삭제
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  번역을_삭제_한다: (
    documentId: string,
    languageId: string,
  ) => Promise<ApiResponse<void>>;

  // ========== 첨부파일 CRUD 메서드 ==========
  /**
   * 첨부파일 목록 조회
   *
   * 특정 번역의 모든 첨부파일을 조회합니다.
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  첨부파일_목록을_조회_한다: (
    documentId: string,
    languageId: string,
  ) => Promise<ApiResponse<HomepageAttachmentDto[]>>;

  /**
   * 첨부파일 조회
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   */
  첨부파일을_조회_한다: (
    documentId: string,
    languageId: string,
    attachmentId: string,
  ) => Promise<ApiResponse<HomepageAttachmentDto>>;

  /**
   * 첨부파일 생성
   *
   * @param data - 생성할 첨부파일 데이터
   */
  첨부파일을_생성_한다: (
    data: CreateHomepageAttachmentDto,
  ) => Promise<ApiResponse<HomepageAttachmentDto>>;

  /**
   * 첨부파일 수정
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  첨부파일을_수정_한다: (
    documentId: string,
    languageId: string,
    attachmentId: string,
    data: UpdateHomepageAttachmentDto,
  ) => Promise<ApiResponse<HomepageAttachmentDto>>;

  /**
   * 첨부파일 삭제
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   */
  첨부파일을_삭제_한다: (
    documentId: string,
    languageId: string,
    attachmentId: string,
  ) => Promise<ApiResponse<void>>;

  // ========== 언어 CRUD 메서드 ==========
  /**
   * 언어 목록 조회
   */
  언어_목록을_조회_한다: () => Promise<ApiResponse<HomepageLanguageDto[]>>;

  /**
   * 언어 생성
   *
   * @param data - 생성할 언어 데이터
   */
  언어를_생성_한다: (
    data: CreateHomepageLanguageDto,
  ) => Promise<ApiResponse<HomepageLanguageDto>>;

  /**
   * 언어 수정
   *
   * @param languageId - 수정할 언어 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  언어를_수정_한다: (
    languageId: string,
    data: UpdateHomepageLanguageDto,
  ) => Promise<ApiResponse<HomepageLanguageDto>>;

  /**
   * 언어 삭제
   *
   * @param languageId - 삭제할 언어 ID
   */
  언어를_삭제_한다: (languageId: string) => Promise<ApiResponse<void>>;
}
```

브로슈어

```typescript
/**
 * 브로슈어 도메인 서비스 인터페이스
 *
 * @description
 * - 브로슈어 문서, 카테고리, 번역, 언어에 대한 통합 CRUD 명세를 정의합니다.
 * - 백엔드 API와의 계약을 추상화하여 Mock 서비스와 실 API 서비스의 교체를 용이하게 합니다.
 */

import { DocumentCode } from '@/cms/admin/homepage/_entities/homepage.entity';
import { ApiResponse } from '@/app/(planning)/plan/cms/_types/common.model';
import type { HomepageLanguageEntity } from '@/cms/admin/homepage/_entities/homepage.entity';
import type {
  BrochureDto,
  BrochureCategoryDto,
  BrochureTranslationDto,
  BrochureAttachmentDto,
  CreateBrochureDto,
  UpdateBrochureDto,
  CreateBrochureCategoryDto,
  UpdateBrochureCategoryDto,
  CreateBrochureTranslationDto,
  UpdateBrochureTranslationDto,
  CreateBrochureAttachmentDto,
  UpdateBrochureAttachmentDto,
} from '../_entities/brochure.dto';
import type {
  CreateHomepageLanguageDto,
  UpdateHomepageLanguageDto,
} from '@/cms/admin/homepage/_entities/homepage.dto';

/**
 * 브로슈어 통합 관리 서비스 명세
 */
export interface BrochureService {
  // ========== 브로슈어 CRUD 메서드 ==========
  /**
   * 브로슈어 목록 조회
   *
   * @param code - CMS 문서 타입 코드 (필수)
   *   - 해당 code로 필터링하여 브로슈어 목록을 조회합니다.
   *   - 카테고리 정보가 포함된 DTO를 반환합니다.
   */
  브로슈어_목록을_조회_한다: (
    code: DocumentCode,
  ) => Promise<ApiResponse<BrochureDto[]>>;

  /**
   * 브로슈어 상세 조회
   *
   * @param documentId - 조회할 브로슈어 ID
   *   - 카테고리 및 번역 정보가 포함된 DTO를 반환합니다.
   */
  브로슈어를_조회_한다: (
    documentId: string,
  ) => Promise<ApiResponse<BrochureDto>>;

  /**
   * 브로슈어 생성
   *
   * @param data - 생성할 브로슈어 데이터
   *   - categoryId를 포함할 수 있으며, 직접 외래키 관계로 카테고리를 연결합니다.
   */
  브로슈어를_생성_한다: (
    data: CreateBrochureDto,
  ) => Promise<ApiResponse<BrochureDto>>;

  /**
   * 브로슈어 수정
   *
   * @param documentId - 수정할 브로슈어 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   *   - categoryId를 포함할 수 있으며, null로 설정 시 카테고리 관계를 제거합니다.
   */
  브로슈어를_수정_한다: (
    documentId: string,
    data: UpdateBrochureDto,
  ) => Promise<ApiResponse<BrochureDto>>;

  /**
   * 브로슈어 삭제
   *
   * 브로슈어를 삭제하면 메타데이터와 관련된 모든 언어별 브로슈어도 함께 삭제됩니다.
   *
   * @param documentId - 삭제할 브로슈어 ID
   */
  브로슈어를_삭제_한다: (documentId: string) => Promise<ApiResponse<void>>;

  // ========== 카테고리 CRUD 메서드 ==========
  /**
   * 카테고리 목록 조회
   *
   * @param code - CMS 문서 타입 코드 (필수)
   *   - 해당 code로 필터링하여 카테고리 목록을 조회합니다.
   */
  카테고리_목록을_조회_한다: (
    code: DocumentCode,
  ) => Promise<ApiResponse<BrochureCategoryDto[]>>;

  /**
   * 카테고리 생성
   *
   * @param data - 생성할 카테고리 데이터
   */
  카테고리를_생성_한다: (
    data: CreateBrochureCategoryDto,
  ) => Promise<ApiResponse<BrochureCategoryDto>>;

  /**
   * 카테고리 수정
   *
   * @param categoryId - 수정할 카테고리 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  카테고리를_수정_한다: (
    categoryId: string,
    data: UpdateBrochureCategoryDto,
  ) => Promise<ApiResponse<BrochureCategoryDto>>;

  /**
   * 카테고리 삭제
   *
   * @param categoryId - 삭제할 카테고리 ID
   */
  카테고리를_삭제_한다: (categoryId: string) => Promise<ApiResponse<void>>;

  // ========== 번역 CRUD 메서드 ==========
  /**
   * 번역 목록 조회
   *
   * 특정 문서의 모든 번역을 조회합니다.
   *
   * @param documentId - 문서 ID
   */
  번역_목록을_조회_한다: (
    documentId: string,
  ) => Promise<ApiResponse<BrochureTranslationDto[]>>;

  /**
   * 번역 조회
   *
   * 특정 문서의 특정 언어 번역을 조회합니다.
   * 언어 및 첨부파일 정보가 포함된 DTO를 반환합니다.
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  번역을_조회_한다: (
    documentId: string,
    languageId: string,
  ) => Promise<ApiResponse<BrochureTranslationDto>>;

  /**
   * 번역 생성
   *
   * @param data - 생성할 번역 데이터
   */
  번역을_생성_한다: (
    data: CreateBrochureTranslationDto,
  ) => Promise<ApiResponse<BrochureTranslationDto>>;

  /**
   * 번역 수정
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  번역을_수정_한다: (
    documentId: string,
    languageId: string,
    data: UpdateBrochureTranslationDto,
  ) => Promise<ApiResponse<BrochureTranslationDto>>;

  /**
   * 번역 삭제
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  번역을_삭제_한다: (
    documentId: string,
    languageId: string,
  ) => Promise<ApiResponse<void>>;

  // ========== 첨부파일 CRUD 메서드 ==========
  /**
   * 첨부파일 목록 조회
   *
   * 특정 번역의 모든 첨부파일을 조회합니다.
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  첨부파일_목록을_조회_한다(
    documentId: string,
    languageId: string,
  ): Promise<ApiResponse<BrochureAttachmentDto[]>>;

  /**
   * 첨부파일 조회
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   */
  첨부파일을_조회_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
  ): Promise<ApiResponse<BrochureAttachmentDto>>;

  /**
   * 첨부파일 생성
   *
   * @param data - 생성할 첨부파일 데이터
   */
  첨부파일을_생성_한다(
    data: CreateBrochureAttachmentDto,
  ): Promise<ApiResponse<BrochureAttachmentDto>>;

  /**
   * 첨부파일 수정
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  첨부파일을_수정_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
    data: UpdateBrochureAttachmentDto,
  ): Promise<ApiResponse<BrochureAttachmentDto>>;

  /**
   * 첨부파일 삭제
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   */
  첨부파일을_삭제_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
  ): Promise<ApiResponse<void>>;

  // ========== 언어 CRUD 메서드 ==========
  /**
   * 언어 목록 조회
   */
  언어_목록을_조회_한다: () => Promise<ApiResponse<HomepageLanguageEntity[]>>;

  /**
   * 언어 생성
   *
   * @param data - 생성할 언어 데이터
   */
  언어를_생성_한다: (
    data: CreateHomepageLanguageDto,
  ) => Promise<ApiResponse<HomepageLanguageEntity>>;

  /**
   * 언어 수정
   *
   * @param languageId - 수정할 언어 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  언어를_수정_한다: (
    languageId: string,
    data: UpdateHomepageLanguageDto,
  ) => Promise<ApiResponse<HomepageLanguageEntity>>;

  /**
   * 언어 삭제
   *
   * @param languageId - 삭제할 언어 ID
   */
  언어를_삭제_한다: (languageId: string) => Promise<ApiResponse<void>>;
}
```

전자공시

```typescript
/**
 * 전자공시 도메인 서비스 인터페이스
 *
 * @description
 * - 전자공시 문서, 카테고리, 번역, 언어에 대한 통합 CRUD 명세를 정의합니다.
 * - 백엔드 API와의 계약을 추상화하여 Mock 서비스와 실 API 서비스의 교체를 용이하게 합니다.
 */

import { DocumentCode } from '@/cms/admin/homepage/_entities/homepage.entity';
import { ApiResponse } from '@/app/(planning)/plan/cms/_types/common.model';
import type {
  ElectronicNoticeDto,
  ElectronicNoticeCategoryDto,
  ElectronicNoticeTranslationDto,
  ElectronicNoticeAttachmentDto,
  CreateElectronicNoticeDto,
  UpdateElectronicNoticeDto,
  CreateElectronicNoticeCategoryDto,
  UpdateElectronicNoticeCategoryDto,
  CreateElectronicNoticeTranslationDto,
  UpdateElectronicNoticeTranslationDto,
  CreateElectronicNoticeAttachmentDto,
  UpdateElectronicNoticeAttachmentDto,
} from '../_entities/electronic-notice.dto';
import type { HomepageLanguageDto } from '@/cms/admin/homepage/_entities/homepage.dto';

/**
 * 전자공시 통합 관리 서비스 명세
 */
export interface ElectronicNoticeService {
  // ========== 전자공시 CRUD 메서드 ==========
  /**
   * 전자공시 목록 조회
   *
   * @param code - CMS 문서 타입 코드 (필수)
   *   - 해당 code로 필터링하여 전자공시 목록을 조회합니다.
   *   - 카테고리 정보가 포함된 DTO를 반환합니다.
   */
  전자공시_목록을_조회_한다: (
    code: DocumentCode,
  ) => Promise<ApiResponse<ElectronicNoticeDto[]>>;

  /**
   * 전자공시 상세 조회
   *
   * @param documentId - 조회할 전자공시 ID
   *   - 카테고리 및 번역 정보가 포함된 DTO를 반환합니다.
   */
  전자공시를_조회_한다: (
    documentId: string,
  ) => Promise<ApiResponse<ElectronicNoticeDto>>;

  /**
   * 전자공시 생성
   *
   * @param data - 생성할 전자공시 데이터
   *   - categoryId를 포함할 수 있으며, 직접 외래키 관계로 카테고리를 연결합니다.
   */
  전자공시를_생성_한다: (
    data: CreateElectronicNoticeDto,
  ) => Promise<ApiResponse<ElectronicNoticeDto>>;

  /**
   * 전자공시 수정
   *
   * @param documentId - 수정할 전자공시 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   *   - categoryId를 포함할 수 있으며, null로 설정 시 카테고리 관계를 제거합니다.
   */
  전자공시를_수정_한다: (
    documentId: string,
    data: UpdateElectronicNoticeDto,
  ) => Promise<ApiResponse<ElectronicNoticeDto>>;

  /**
   * 전자공시 삭제
   *
   * 전자공시를 삭제하면 메타데이터와 관련된 모든 언어별 전자공시도 함께 삭제됩니다.
   *
   * @param documentId - 삭제할 전자공시 ID
   */
  전자공시를_삭제_한다: (documentId: string) => Promise<ApiResponse<void>>;

  // ========== 카테고리 CRUD 메서드 ==========
  /**
   * 카테고리 목록 조회
   *
   * @param code - CMS 문서 타입 코드 (필수)
   *   - 해당 code로 필터링하여 카테고리 목록을 조회합니다.
   */
  카테고리_목록을_조회_한다: (
    code: DocumentCode,
  ) => Promise<ApiResponse<ElectronicNoticeCategoryDto[]>>;

  /**
   * 카테고리 생성
   *
   * @param data - 생성할 카테고리 데이터
   */
  카테고리를_생성_한다: (
    data: CreateElectronicNoticeCategoryDto,
  ) => Promise<ApiResponse<ElectronicNoticeCategoryDto>>;

  /**
   * 카테고리 수정
   *
   * @param categoryId - 수정할 카테고리 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  카테고리를_수정_한다: (
    categoryId: string,
    data: UpdateElectronicNoticeCategoryDto,
  ) => Promise<ApiResponse<ElectronicNoticeCategoryDto>>;

  /**
   * 카테고리 삭제
   *
   * @param categoryId - 삭제할 카테고리 ID
   */
  카테고리를_삭제_한다: (categoryId: string) => Promise<ApiResponse<void>>;

  // ========== 번역 CRUD 메서드 ==========
  /**
   * 번역 목록 조회
   *
   * 특정 문서의 모든 번역을 조회합니다.
   *
   * @param documentId - 문서 ID
   */
  번역_목록을_조회_한다: (
    documentId: string,
  ) => Promise<ApiResponse<ElectronicNoticeTranslationDto[]>>;

  /**
   * 번역 조회
   *
   * 특정 문서의 특정 언어 번역을 조회합니다.
   * 언어 및 첨부파일 정보가 포함된 DTO를 반환합니다.
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  번역을_조회_한다: (
    documentId: string,
    languageId: string,
  ) => Promise<ApiResponse<ElectronicNoticeTranslationDto>>;

  /**
   * 번역 생성
   *
   * @param data - 생성할 번역 데이터
   */
  번역을_생성_한다: (
    data: CreateElectronicNoticeTranslationDto,
  ) => Promise<ApiResponse<ElectronicNoticeTranslationDto>>;

  /**
   * 번역 수정
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  번역을_수정_한다: (
    documentId: string,
    languageId: string,
    data: UpdateElectronicNoticeTranslationDto,
  ) => Promise<ApiResponse<ElectronicNoticeTranslationDto>>;

  /**
   * 번역 삭제
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  번역을_삭제_한다: (
    documentId: string,
    languageId: string,
  ) => Promise<ApiResponse<void>>;

  // ========== 첨부파일 CRUD 메서드 ==========
  /**
   * 첨부파일 목록 조회
   *
   * 특정 번역의 모든 첨부파일을 조회합니다.
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  첨부파일_목록을_조회_한다(
    documentId: string,
    languageId: string,
  ): Promise<ApiResponse<ElectronicNoticeAttachmentDto[]>>;

  /**
   * 첨부파일 조회
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   */
  첨부파일을_조회_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
  ): Promise<ApiResponse<ElectronicNoticeAttachmentDto>>;

  /**
   * 첨부파일 생성
   *
   * @param data - 생성할 첨부파일 데이터
   */
  첨부파일을_생성_한다(
    data: CreateElectronicNoticeAttachmentDto,
  ): Promise<ApiResponse<ElectronicNoticeAttachmentDto>>;

  /**
   * 첨부파일 수정
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  첨부파일을_수정_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
    data: UpdateElectronicNoticeAttachmentDto,
  ): Promise<ApiResponse<ElectronicNoticeAttachmentDto>>;

  /**
   * 첨부파일 삭제
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   */
  첨부파일을_삭제_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
  ): Promise<ApiResponse<void>>;

  // ========== 언어 관련 ==========
  /**
   * 언어 목록 조회
   */
  언어_목록을_조회_한다: () => Promise<ApiResponse<HomepageLanguageDto[]>>;
}
```

IR

```typescript
/**
 * IR 도메인 서비스 인터페이스
 *
 * @description
 * - IR 문서, 카테고리, 번역, 언어에 대한 통합 CRUD 명세를 정의합니다.
 * - 백엔드 API와의 계약을 추상화하여 Mock 서비스와 실 API 서비스의 교체를 용이하게 합니다.
 */

import { DocumentCode } from '@/cms/admin/homepage/_entities/homepage.entity';
import { ApiResponse } from '@/app/(planning)/plan/cms/_types/common.model';
import type {
  IrDto,
  IrCategoryDto,
  IrTranslationDto,
  IrAttachmentDto,
  CreateIrDto,
  UpdateIrDto,
  CreateIrCategoryDto,
  UpdateIrCategoryDto,
  CreateIrTranslationDto,
  UpdateIrTranslationDto,
  CreateIrAttachmentDto,
  UpdateIrAttachmentDto,
} from '../_entities/ir.dto';
import type { HomepageLanguageEntity } from '@/cms/admin/homepage/_entities/homepage.entity';
import type {
  CreateHomepageLanguageDto,
  UpdateHomepageLanguageDto,
} from '@/cms/admin/homepage/_entities/homepage.dto';

/**
 * IR 통합 관리 서비스 명세
 */
export interface IrService {
  // ========== IR CRUD 메서드 ==========
  /**
   * IR 목록 조회
   *
   * @param code - CMS 문서 타입 코드 (필수)
   *   - 해당 code로 필터링하여 IR 목록을 조회합니다.
   *   - 카테고리 정보가 포함된 DTO를 반환합니다.
   */
  IR_목록을_조회_한다: (code: DocumentCode) => Promise<ApiResponse<IrDto[]>>;

  /**
   * IR 상세 조회
   *
   * @param documentId - 조회할 IR ID
   *   - 카테고리 및 번역 정보가 포함된 DTO를 반환합니다.
   */
  IR을_조회_한다: (documentId: string) => Promise<ApiResponse<IrDto>>;

  /**
   * IR 생성
   *
   * @param data - 생성할 IR 데이터
   *   - categoryId를 포함할 수 있으며, 직접 외래키 관계로 카테고리를 연결합니다.
   */
  IR을_생성_한다: (data: CreateIrDto) => Promise<ApiResponse<IrDto>>;

  /**
   * IR 수정
   *
   * @param documentId - 수정할 IR ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   *   - categoryId를 포함할 수 있으며, null로 설정 시 카테고리 관계를 제거합니다.
   */
  IR을_수정_한다: (
    documentId: string,
    data: UpdateIrDto,
  ) => Promise<ApiResponse<IrDto>>;

  /**
   * IR 삭제
   *
   * IR을 삭제하면 메타데이터와 관련된 모든 언어별 IR도 함께 삭제됩니다.
   *
   * @param documentId - 삭제할 IR ID
   */
  IR을_삭제_한다: (documentId: string) => Promise<ApiResponse<void>>;

  // ========== 카테고리 CRUD 메서드 ==========
  /**
   * 카테고리 목록 조회
   *
   * @param code - CMS 문서 타입 코드 (필수)
   *   - 해당 code로 필터링하여 카테고리 목록을 조회합니다.
   */
  카테고리_목록을_조회_한다: (
    code: DocumentCode,
  ) => Promise<ApiResponse<IrCategoryDto[]>>;

  /**
   * 카테고리 생성
   *
   * @param data - 생성할 카테고리 데이터
   */
  카테고리를_생성_한다: (
    data: CreateIrCategoryDto,
  ) => Promise<ApiResponse<IrCategoryDto>>;

  /**
   * 카테고리 수정
   *
   * @param categoryId - 수정할 카테고리 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  카테고리를_수정_한다: (
    categoryId: string,
    data: UpdateIrCategoryDto,
  ) => Promise<ApiResponse<IrCategoryDto>>;

  /**
   * 카테고리 삭제
   *
   * @param categoryId - 삭제할 카테고리 ID
   */
  카테고리를_삭제_한다: (categoryId: string) => Promise<ApiResponse<void>>;

  // ========== 번역 CRUD 메서드 ==========
  /**
   * 번역 목록 조회
   *
   * 특정 문서의 모든 번역을 조회합니다.
   *
   * @param documentId - 문서 ID
   */
  번역_목록을_조회_한다: (
    documentId: string,
  ) => Promise<ApiResponse<IrTranslationDto[]>>;

  /**
   * 번역 조회
   *
   * 특정 문서의 특정 언어 번역을 조회합니다.
   * 언어 및 첨부파일 정보가 포함된 DTO를 반환합니다.
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  번역을_조회_한다: (
    documentId: string,
    languageId: string,
  ) => Promise<ApiResponse<IrTranslationDto>>;

  /**
   * 번역 생성
   *
   * @param data - 생성할 번역 데이터
   */
  번역을_생성_한다: (
    data: CreateIrTranslationDto,
  ) => Promise<ApiResponse<IrTranslationDto>>;

  /**
   * 번역 수정
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  번역을_수정_한다: (
    documentId: string,
    languageId: string,
    data: UpdateIrTranslationDto,
  ) => Promise<ApiResponse<IrTranslationDto>>;

  /**
   * 번역 삭제
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  번역을_삭제_한다: (
    documentId: string,
    languageId: string,
  ) => Promise<ApiResponse<void>>;

  // ========== 첨부파일 CRUD 메서드 ==========
  /**
   * 첨부파일 목록 조회
   *
   * 특정 번역의 모든 첨부파일을 조회합니다.
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  첨부파일_목록을_조회_한다(
    documentId: string,
    languageId: string,
  ): Promise<ApiResponse<IrAttachmentDto[]>>;

  /**
   * 첨부파일 조회
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   */
  첨부파일을_조회_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
  ): Promise<ApiResponse<IrAttachmentDto>>;

  /**
   * 첨부파일 생성
   *
   * @param data - 생성할 첨부파일 데이터
   */
  첨부파일을_생성_한다(
    data: CreateIrAttachmentDto,
  ): Promise<ApiResponse<IrAttachmentDto>>;

  /**
   * 첨부파일 수정
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  첨부파일을_수정_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
    data: UpdateIrAttachmentDto,
  ): Promise<ApiResponse<IrAttachmentDto>>;

  /**
   * 첨부파일 삭제
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   */
  첨부파일을_삭제_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
  ): Promise<ApiResponse<void>>;

  // ========== 언어 CRUD 메서드 ==========
  /**
   * 언어 목록 조회
   */
  언어_목록을_조회_한다: () => Promise<ApiResponse<HomepageLanguageEntity[]>>;

  /**
   * 언어 생성
   *
   * @param data - 생성할 언어 데이터
   */
  언어를_생성_한다: (
    data: CreateHomepageLanguageDto,
  ) => Promise<ApiResponse<HomepageLanguageEntity>>;

  /**
   * 언어 수정
   *
   * @param languageId - 수정할 언어 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  언어를_수정_한다: (
    languageId: string,
    data: UpdateHomepageLanguageDto,
  ) => Promise<ApiResponse<HomepageLanguageEntity>>;

  /**
   * 언어 삭제
   *
   * @param languageId - 삭제할 언어 ID
   */
  언어를_삭제_한다: (languageId: string) => Promise<ApiResponse<void>>;
}
```

팝업

```typescript
import { ApiResponse } from '@/app/(planning)/plan/cms/_types/common.model';
import { DocumentCode } from '@/cms/admin/homepage/_entities/homepage.entity';
import {
  PopupDto,
  PopupCategoryDto,
  PopupTranslationDto,
  PopupAttachmentDto,
  CreatePopupDto,
  UpdatePopupDto,
  CreatePopupCategoryDto,
  UpdatePopupCategoryDto,
  CreatePopupTranslationDto,
  UpdatePopupTranslationDto,
  CreatePopupAttachmentDto,
  UpdatePopupAttachmentDto,
} from '../_entities/popup.dto';
import { HomepageLanguageEntity } from '@/cms/admin/homepage/_entities/homepage.entity';
import {
  CreateHomepageLanguageDto,
  UpdateHomepageLanguageDto,
} from '@/cms/admin/homepage/_entities/homepage.dto';

/**
 * 팝업 도메인 서비스 인터페이스
 *
 * @description
 * - 팝업 문서, 카테고리, 번역, 첨부파일, 언어에 대한 통합 CRUD 명세를 정의합니다.
 * - 백엔드 API와의 계약을 추상화하여 Mock 서비스와 실 API 서비스의 교체를 용이하게 합니다.
 */
export interface PopupService {
  // ========== 팝업 문서 CRUD 메서드 ==========
  /**
   * 팝업 목록 조회
   *
   * @param code - CMS 문서 타입 코드 (필수)
   *   - 해당 code로 필터링하여 팝업 목록을 조회합니다.
   *   - 카테고리 정보가 포함된 DTO를 반환합니다.
   */
  팝업_목록을_조회_한다(code: DocumentCode): Promise<ApiResponse<PopupDto[]>>;

  /**
   * 팝업 상세 조회
   *
   * @param documentId - 조회할 팝업 ID
   *   - 카테고리 및 번역 정보가 포함된 DTO를 반환합니다.
   */
  팝업을_조회_한다(documentId: string): Promise<ApiResponse<PopupDto>>;

  /**
   * 팝업 생성
   *
   * @param data - 생성할 팝업 데이터
   *   - categoryId를 포함할 수 있으며, 직접 외래키 관계로 카테고리를 연결합니다.
   */
  팝업을_생성_한다(data: CreatePopupDto): Promise<ApiResponse<PopupDto>>;

  /**
   * 팝업 수정
   *
   * @param documentId - 수정할 팝업 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   *   - categoryId를 포함할 수 있으며, null로 설정 시 카테고리 관계를 제거합니다.
   */
  팝업을_수정_한다(
    documentId: string,
    data: UpdatePopupDto,
  ): Promise<ApiResponse<PopupDto>>;

  /**
   * 팝업 삭제
   *
   * 팝업을 삭제하면 메타데이터와 관련된 모든 언어별 팝업도 함께 삭제됩니다.
   *
   * @param documentId - 삭제할 팝업 ID
   */
  팝업을_삭제_한다(documentId: string): Promise<ApiResponse<void>>;

  // ========== 카테고리 CRUD 메서드 ==========
  /**
   * 카테고리 목록 조회
   *
   * @param code - CMS 문서 타입 코드 (필수)
   *   - 해당 code로 필터링하여 카테고리 목록을 조회합니다.
   */
  카테고리_목록을_조회_한다(
    code: DocumentCode,
  ): Promise<ApiResponse<PopupCategoryDto[]>>;

  /**
   * 카테고리 생성
   *
   * @param data - 생성할 카테고리 데이터
   */
  카테고리를_생성_한다(
    data: CreatePopupCategoryDto,
  ): Promise<ApiResponse<PopupCategoryDto>>;

  /**
   * 카테고리 수정
   *
   * @param categoryId - 수정할 카테고리 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  카테고리를_수정_한다(
    categoryId: string,
    data: UpdatePopupCategoryDto,
  ): Promise<ApiResponse<PopupCategoryDto>>;

  /**
   * 카테고리 삭제
   *
   * @param categoryId - 삭제할 카테고리 ID
   */
  카테고리를_삭제_한다(categoryId: string): Promise<ApiResponse<void>>;

  // ========== 번역 CRUD 메서드 ==========
  /**
   * 번역 목록 조회
   *
   * 특정 문서의 모든 번역을 조회합니다.
   *
   * @param documentId - 문서 ID
   */
  번역_목록을_조회_한다(
    documentId: string,
  ): Promise<ApiResponse<PopupTranslationDto[]>>;

  /**
   * 번역 조회
   *
   * 특정 문서의 특정 언어 번역을 조회합니다.
   * 언어 및 첨부파일 정보가 포함된 DTO를 반환합니다.
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  번역을_조회_한다(
    documentId: string,
    languageId: string,
  ): Promise<ApiResponse<PopupTranslationDto>>;

  /**
   * 번역 생성
   *
   * @param data - 생성할 번역 데이터
   */
  번역을_생성_한다(
    data: CreatePopupTranslationDto,
  ): Promise<ApiResponse<PopupTranslationDto>>;

  /**
   * 번역 수정
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  번역을_수정_한다(
    documentId: string,
    languageId: string,
    data: UpdatePopupTranslationDto,
  ): Promise<ApiResponse<PopupTranslationDto>>;

  /**
   * 번역 삭제
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  번역을_삭제_한다(
    documentId: string,
    languageId: string,
  ): Promise<ApiResponse<void>>;

  // ========== 첨부파일 CRUD 메서드 ==========

  /**
   * 첨부파일 목록 조회
   *
   * 특정 번역의 모든 첨부파일을 조회합니다.
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  첨부파일_목록을_조회_한다(
    documentId: string,
    languageId: string,
  ): Promise<ApiResponse<PopupAttachmentDto[]>>;

  /**
   * 첨부파일 조회
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   */
  첨부파일을_조회_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
  ): Promise<ApiResponse<PopupAttachmentDto>>;

  /**
   * 첨부파일 생성
   *
   * @param data - 생성할 첨부파일 데이터
   */
  첨부파일을_생성_한다(
    data: CreatePopupAttachmentDto,
  ): Promise<ApiResponse<PopupAttachmentDto>>;

  /**
   * 첨부파일 수정
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  첨부파일을_수정_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
    data: UpdatePopupAttachmentDto,
  ): Promise<ApiResponse<PopupAttachmentDto>>;

  /**
   * 첨부파일 삭제
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   */
  첨부파일을_삭제_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
  ): Promise<ApiResponse<void>>;

  // ========== 언어 CRUD 메서드 ==========
  /**
   * 언어 목록 조회
   */
  언어_목록을_조회_한다(): Promise<ApiResponse<HomepageLanguageEntity[]>>;

  /**
   * 언어 생성
   *
   * @param data - 생성할 언어 데이터
   */
  언어를_생성_한다(
    data: CreateHomepageLanguageDto,
  ): Promise<ApiResponse<HomepageLanguageEntity>>;

  /**
   * 언어 수정
   *
   * @param languageId - 수정할 언어 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  언어를_수정_한다(
    languageId: string,
    data: UpdateHomepageLanguageDto,
  ): Promise<ApiResponse<HomepageLanguageEntity>>;

  /**
   * 언어 삭제
   *
   * @param languageId - 삭제할 언어 ID
   */
  언어를_삭제_한다(languageId: string): Promise<ApiResponse<void>>;
}
```

주주총회

```typescript
/**
 * 주주총회 도메인 서비스 인터페이스
 *
 * @description
 * - 주주총회 문서, 카테고리, 번역, 언어에 대한 통합 CRUD 명세를 정의합니다.
 * - 백엔드 API와의 계약을 추상화하여 Mock 서비스와 실 API 서비스의 교체를 용이하게 합니다.
 */

import { DocumentCode } from '@/cms/admin/homepage/_entities/homepage.entity';
import { ApiResponse } from '@/app/(planning)/plan/cms/_types/common.model';
import type { HomepageLanguageEntity } from '@/cms/admin/homepage/_entities/homepage.entity';
import type {
  ShareholdersMeetingDto,
  ShareholdersMeetingCategoryDto,
  ShareholdersMeetingTranslationDto,
  ShareholdersMeetingAttachmentDto,
  ShareholdersMeetingDetailDto,
  AgendaItemDto,
  CreateShareholdersMeetingDto,
  UpdateShareholdersMeetingDto,
  CreateShareholdersMeetingCategoryDto,
  UpdateShareholdersMeetingCategoryDto,
  CreateShareholdersMeetingTranslationDto,
  UpdateShareholdersMeetingTranslationDto,
  CreateShareholdersMeetingAttachmentDto,
  UpdateShareholdersMeetingAttachmentDto,
  CreateShareholdersMeetingDetailDto,
  UpdateShareholdersMeetingDetailDto,
  CreateAgendaItemDto,
  UpdateAgendaItemDto,
} from '../_entities/shareholders-meeting.dto';
import type {
  CreateHomepageLanguageDto,
  UpdateHomepageLanguageDto,
} from '@/cms/admin/homepage/_entities/homepage.dto';

/**
 * 주주총회 통합 관리 서비스 명세
 */
export interface ShareholdersMeetingService {
  // ========== 주주총회 CRUD 메서드 ==========
  /**
   * 주주총회 목록 조회
   *
   * @param code - CMS 문서 타입 코드 (필수)
   *   - 해당 code로 필터링하여 주주총회 목록을 조회합니다.
   *   - 카테고리 정보가 포함된 DTO를 반환합니다.
   */
  주주총회_목록을_조회_한다: (
    code: DocumentCode,
  ) => Promise<ApiResponse<ShareholdersMeetingDto[]>>;

  /**
   * 주주총회 상세 조회
   *
   * @param documentId - 조회할 주주총회 ID
   *   - 카테고리 및 번역 정보가 포함된 DTO를 반환합니다.
   */
  주주총회를_조회_한다: (
    documentId: string,
  ) => Promise<ApiResponse<ShareholdersMeetingDto>>;

  /**
   * 주주총회 생성
   *
   * @param data - 생성할 주주총회 데이터
   *   - categoryId를 포함할 수 있으며, 직접 외래키 관계로 카테고리를 연결합니다.
   */
  주주총회를_생성_한다: (
    data: CreateShareholdersMeetingDto,
  ) => Promise<ApiResponse<ShareholdersMeetingDto>>;

  /**
   * 주주총회 수정
   *
   * @param documentId - 수정할 주주총회 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   *   - categoryId를 포함할 수 있으며, null로 설정 시 카테고리 관계를 제거합니다.
   */
  주주총회를_수정_한다: (
    documentId: string,
    data: UpdateShareholdersMeetingDto,
  ) => Promise<ApiResponse<ShareholdersMeetingDto>>;

  /**
   * 주주총회 삭제
   *
   * 주주총회를 삭제하면 메타데이터와 관련된 모든 언어별 주주총회도 함께 삭제됩니다.
   *
   * @param documentId - 삭제할 주주총회 ID
   */
  주주총회를_삭제_한다: (documentId: string) => Promise<ApiResponse<void>>;

  // ========== 카테고리 CRUD 메서드 ==========
  /**
   * 카테고리 목록 조회
   *
   * @param code - CMS 문서 타입 코드 (필수)
   *   - 해당 code로 필터링하여 카테고리 목록을 조회합니다.
   */
  카테고리_목록을_조회_한다: (
    code: DocumentCode,
  ) => Promise<ApiResponse<ShareholdersMeetingCategoryDto[]>>;

  /**
   * 카테고리 생성
   *
   * @param data - 생성할 카테고리 데이터
   */
  카테고리를_생성_한다: (
    data: CreateShareholdersMeetingCategoryDto,
  ) => Promise<ApiResponse<ShareholdersMeetingCategoryDto>>;

  /**
   * 카테고리 수정
   *
   * @param categoryId - 수정할 카테고리 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  카테고리를_수정_한다: (
    categoryId: string,
    data: UpdateShareholdersMeetingCategoryDto,
  ) => Promise<ApiResponse<ShareholdersMeetingCategoryDto>>;

  /**
   * 카테고리 삭제
   *
   * @param categoryId - 삭제할 카테고리 ID
   */
  카테고리를_삭제_한다: (categoryId: string) => Promise<ApiResponse<void>>;

  // ========== 번역 CRUD 메서드 ==========
  /**
   * 번역 목록 조회
   *
   * 특정 문서의 모든 번역을 조회합니다.
   *
   * @param documentId - 문서 ID
   */
  번역_목록을_조회_한다: (
    documentId: string,
  ) => Promise<ApiResponse<ShareholdersMeetingTranslationDto[]>>;

  /**
   * 번역 조회
   *
   * 특정 문서의 특정 언어 번역을 조회합니다.
   * 언어 및 첨부파일 정보가 포함된 DTO를 반환합니다.
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  번역을_조회_한다: (
    documentId: string,
    languageId: string,
  ) => Promise<ApiResponse<ShareholdersMeetingTranslationDto>>;

  /**
   * 번역 생성
   *
   * @param data - 생성할 번역 데이터
   */
  번역을_생성_한다: (
    data: CreateShareholdersMeetingTranslationDto,
  ) => Promise<ApiResponse<ShareholdersMeetingTranslationDto>>;

  /**
   * 번역 수정
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  번역을_수정_한다: (
    documentId: string,
    languageId: string,
    data: UpdateShareholdersMeetingTranslationDto,
  ) => Promise<ApiResponse<ShareholdersMeetingTranslationDto>>;

  /**
   * 번역 삭제
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  번역을_삭제_한다: (
    documentId: string,
    languageId: string,
  ) => Promise<ApiResponse<void>>;

  // ========== 첨부파일 CRUD 메서드 ==========
  /**
   * 첨부파일 목록 조회
   *
   * 특정 번역의 모든 첨부파일을 조회합니다.
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  첨부파일_목록을_조회_한다(
    documentId: string,
    languageId: string,
  ): Promise<ApiResponse<ShareholdersMeetingAttachmentDto[]>>;

  /**
   * 첨부파일 조회
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   */
  첨부파일을_조회_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
  ): Promise<ApiResponse<ShareholdersMeetingAttachmentDto>>;

  /**
   * 첨부파일 생성
   *
   * @param data - 생성할 첨부파일 데이터
   */
  첨부파일을_생성_한다(
    data: CreateShareholdersMeetingAttachmentDto,
  ): Promise<ApiResponse<ShareholdersMeetingAttachmentDto>>;

  /**
   * 첨부파일 수정
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  첨부파일을_수정_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
    data: UpdateShareholdersMeetingAttachmentDto,
  ): Promise<ApiResponse<ShareholdersMeetingAttachmentDto>>;

  /**
   * 첨부파일 삭제
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   */
  첨부파일을_삭제_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
  ): Promise<ApiResponse<void>>;

  // ========== 언어 CRUD 메서드 ==========
  /**
   * 언어 목록 조회
   */
  언어_목록을_조회_한다: () => Promise<ApiResponse<HomepageLanguageEntity[]>>;

  /**
   * 언어 생성
   *
   * @param data - 생성할 언어 데이터
   */
  언어를_생성_한다: (
    data: CreateHomepageLanguageDto,
  ) => Promise<ApiResponse<HomepageLanguageEntity>>;

  /**
   * 언어 수정
   *
   * @param languageId - 수정할 언어 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  언어를_수정_한다: (
    languageId: string,
    data: UpdateHomepageLanguageDto,
  ) => Promise<ApiResponse<HomepageLanguageEntity>>;

  /**
   * 언어 삭제
   *
   * @param languageId - 삭제할 언어 ID
   */
  언어를_삭제_한다: (languageId: string) => Promise<ApiResponse<void>>;

  // ========== 주주총회 상세 정보 CRUD 메서드 ==========
  /**
   * 주주총회 상세 정보 조회
   *
   * 특정 문서의 특정 언어 상세 정보를 조회합니다.
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  상세_정보를_조회_한다: (
    documentId: string,
    languageId: string,
  ) => Promise<ApiResponse<ShareholdersMeetingDetailDto>>;

  /**
   * 주주총회 상세 정보 생성
   *
   * @param data - 생성할 상세 정보 데이터
   */
  상세_정보를_생성_한다: (
    data: CreateShareholdersMeetingDetailDto,
  ) => Promise<ApiResponse<ShareholdersMeetingDetailDto>>;

  /**
   * 주주총회 상세 정보 수정
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  상세_정보를_수정_한다: (
    documentId: string,
    languageId: string,
    data: UpdateShareholdersMeetingDetailDto,
  ) => Promise<ApiResponse<ShareholdersMeetingDetailDto>>;

  /**
   * 주주총회 상세 정보 삭제
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  상세_정보를_삭제_한다: (
    documentId: string,
    languageId: string,
  ) => Promise<ApiResponse<void>>;

  // ========== 안건 항목 CRUD 메서드 ==========
  /**
   * 안건 항목 목록 조회
   *
   * 특정 번역의 모든 안건 항목을 조회합니다.
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  안건_항목_목록을_조회_한다: (
    documentId: string,
    languageId: string,
  ) => Promise<ApiResponse<AgendaItemDto[]>>;

  /**
   * 안건 항목 조회
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param agendaItemId - 안건 항목 ID
   */
  안건_항목을_조회_한다: (
    documentId: string,
    languageId: string,
    agendaItemId: string,
  ) => Promise<ApiResponse<AgendaItemDto>>;

  /**
   * 안건 항목 생성
   *
   * @param data - 생성할 안건 항목 데이터
   */
  안건_항목을_생성_한다: (
    data: CreateAgendaItemDto,
  ) => Promise<ApiResponse<AgendaItemDto>>;

  /**
   * 안건 항목 수정
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param agendaItemId - 안건 항목 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  안건_항목을_수정_한다: (
    documentId: string,
    languageId: string,
    agendaItemId: string,
    data: UpdateAgendaItemDto,
  ) => Promise<ApiResponse<AgendaItemDto>>;

  /**
   * 안건 항목 삭제
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param agendaItemId - 안건 항목 ID
   */
  안건_항목을_삭제_한다: (
    documentId: string,
    languageId: string,
    agendaItemId: string,
  ) => Promise<ApiResponse<void>>;
}
```

```루미르 스토리
import { ApiResponse } from "@/app/(planning)/plan/cms/_types/common.model";
import { DocumentCode } from "@/cms/admin/homepage/_entities/homepage.entity";
import {
  StoryDto,
  StoryCategoryDto,
  StoryTranslationDto,
  StoryAttachmentDto,
  CreateStoryDto,
  UpdateStoryDto,
  CreateStoryCategoryDto,
  UpdateStoryCategoryDto,
  CreateStoryTranslationDto,
  UpdateStoryTranslationDto,
  CreateStoryAttachmentDto,
  UpdateStoryAttachmentDto,
} from "../_entities/story.dto";
import { HomepageLanguageEntity } from "@/cms/admin/homepage/_entities/homepage.entity";
import {
  CreateHomepageLanguageDto,
  UpdateHomepageLanguageDto,
} from "@/cms/admin/homepage/_entities/homepage.dto";

/**
 * 스토리 도메인 서비스 인터페이스
 *
 * @description
 * - 스토리 문서, 카테고리, 번역, 첨부파일, 언어에 대한 통합 CRUD 명세를 정의합니다.
 * - 백엔드 API와의 계약을 추상화하여 Mock 서비스와 실 API 서비스의 교체를 용이하게 합니다.
 */
export interface StoryService {
  // ========== 스토리 문서 CRUD 메서드 ==========
  /**
   * 스토리 목록 조회
   *
   * @param code - CMS 문서 타입 코드 (필수)
   *   - 해당 code로 필터링하여 스토리 목록을 조회합니다.
   *   - 카테고리 정보가 포함된 DTO를 반환합니다.
   */
  스토리_목록을_조회_한다(code: DocumentCode): Promise<ApiResponse<StoryDto[]>>;

  /**
   * 스토리 상세 조회
   *
   * @param documentId - 조회할 스토리 ID
   *   - 카테고리 및 번역 정보가 포함된 DTO를 반환합니다.
   */
  스토리를_조회_한다(documentId: string): Promise<ApiResponse<StoryDto>>;

  /**
   * 스토리 생성
   *
   * @param data - 생성할 스토리 데이터
   *   - categoryId를 포함할 수 있으며, 직접 외래키 관계로 카테고리를 연결합니다.
   */
  스토리를_생성_한다(data: CreateStoryDto): Promise<ApiResponse<StoryDto>>;

  /**
   * 스토리 수정
   *
   * @param documentId - 수정할 스토리 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   *   - categoryId를 포함할 수 있으며, null로 설정 시 카테고리 관계를 제거합니다.
   */
  스토리를_수정_한다(
    documentId: string,
    data: UpdateStoryDto
  ): Promise<ApiResponse<StoryDto>>;

  /**
   * 스토리 삭제
   *
   * 스토리을 삭제하면 메타데이터와 관련된 모든 언어별 스토리도 함께 삭제됩니다.
   *
   * @param documentId - 삭제할 스토리 ID
   */
  스토리를_삭제_한다(documentId: string): Promise<ApiResponse<void>>;

  // ========== 카테고리 CRUD 메서드 ==========
  /**
   * 카테고리 목록 조회
   *
   * @param code - CMS 문서 타입 코드 (필수)
   *   - 해당 code로 필터링하여 카테고리 목록을 조회합니다.
   */
  카테고리_목록을_조회_한다(
    code: DocumentCode
  ): Promise<ApiResponse<StoryCategoryDto[]>>;

  /**
   * 카테고리 생성
   *
   * @param data - 생성할 카테고리 데이터
   */
  카테고리를_생성_한다(
    data: CreateStoryCategoryDto
  ): Promise<ApiResponse<StoryCategoryDto>>;

  /**
   * 카테고리 수정
   *
   * @param categoryId - 수정할 카테고리 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  카테고리를_수정_한다(
    categoryId: string,
    data: UpdateStoryCategoryDto
  ): Promise<ApiResponse<StoryCategoryDto>>;

  /**
   * 카테고리 삭제
   *
   * @param categoryId - 삭제할 카테고리 ID
   */
  카테고리를_삭제_한다(categoryId: string): Promise<ApiResponse<void>>;

  // ========== 번역 CRUD 메서드 ==========
  /**
   * 번역 목록 조회
   *
   * 특정 문서의 모든 번역을 조회합니다.
   *
   * @param documentId - 문서 ID
   */
  번역_목록을_조회_한다(
    documentId: string
  ): Promise<ApiResponse<StoryTranslationDto[]>>;

  /**
   * 번역 조회
   *
   * 특정 문서의 특정 언어 번역을 조회합니다.
   * 언어 및 첨부파일 정보가 포함된 DTO를 반환합니다.
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  번역을_조회_한다(
    documentId: string,
    languageId: string
  ): Promise<ApiResponse<StoryTranslationDto>>;

  /**
   * 번역 생성
   *
   * @param data - 생성할 번역 데이터
   */
  번역을_생성_한다(
    data: CreateStoryTranslationDto
  ): Promise<ApiResponse<StoryTranslationDto>>;

  /**
   * 번역 수정
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  번역을_수정_한다(
    documentId: string,
    languageId: string,
    data: UpdateStoryTranslationDto
  ): Promise<ApiResponse<StoryTranslationDto>>;

  /**
   * 번역 삭제
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  번역을_삭제_한다(
    documentId: string,
    languageId: string
  ): Promise<ApiResponse<void>>;

  // ========== 첨부파일 CRUD 메서드 ==========

  /**
   * 첨부파일 목록 조회
   *
   * 특정 번역의 모든 첨부파일을 조회합니다.
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  첨부파일_목록을_조회_한다(
    documentId: string,
    languageId: string
  ): Promise<ApiResponse<StoryAttachmentDto[]>>;

  /**
   * 첨부파일 조회
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   */
  첨부파일을_조회_한다(
    documentId: string,
    languageId: string,
    attachmentId: string
  ): Promise<ApiResponse<StoryAttachmentDto>>;

  /**
   * 첨부파일 생성
   *
   * @param data - 생성할 첨부파일 데이터
   */
  첨부파일을_생성_한다(
    data: CreateStoryAttachmentDto
  ): Promise<ApiResponse<StoryAttachmentDto>>;

  /**
   * 첨부파일 수정
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  첨부파일을_수정_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
    data: UpdateStoryAttachmentDto
  ): Promise<ApiResponse<StoryAttachmentDto>>;

  /**
   * 첨부파일 삭제
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   */
  첨부파일을_삭제_한다(
    documentId: string,
    languageId: string,
    attachmentId: string
  ): Promise<ApiResponse<void>>;

  // ========== 언어 CRUD 메서드 ==========
  /**
   * 언어 목록 조회
   */
  언어_목록을_조회_한다(): Promise<ApiResponse<HomepageLanguageEntity[]>>;

  /**
   * 언어 생성
   *
   * @param data - 생성할 언어 데이터
   */
  언어를_생성_한다(
    data: CreateHomepageLanguageDto
  ): Promise<ApiResponse<HomepageLanguageEntity>>;

  /**
   * 언어 수정
   *
   * @param languageId - 수정할 언어 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  언어를_수정_한다(
    languageId: string,
    data: UpdateHomepageLanguageDto
  ): Promise<ApiResponse<HomepageLanguageEntity>>;

  /**
   * 언어 삭제
   *
   * @param languageId - 삭제할 언어 ID
   */
  언어를_삭제_한다(languageId: string): Promise<ApiResponse<void>>;
}

```

비디오 갤러리

```typescript
import { ApiResponse } from '@/app/(planning)/plan/cms/_types/common.model';
import { DocumentCode } from '@/cms/admin/homepage/_entities/homepage.entity';
import {
  VideoGalleryDto,
  VideoGalleryCategoryDto,
  VideoGalleryTranslationDto,
  VideoGalleryAttachmentDto,
  CreateVideoGalleryDto,
  UpdateVideoGalleryDto,
  CreateVideoGalleryCategoryDto,
  UpdateVideoGalleryCategoryDto,
  CreateVideoGalleryTranslationDto,
  UpdateVideoGalleryTranslationDto,
  CreateVideoGalleryAttachmentDto,
  UpdateVideoGalleryAttachmentDto,
} from '../_entities/video-gallery.dto';
import { HomepageLanguageEntity } from '@/cms/admin/homepage/_entities/homepage.entity';
import {
  CreateHomepageLanguageDto,
  UpdateHomepageLanguageDto,
} from '@/cms/admin/homepage/_entities/homepage.dto';

/**
 * 비디오 갤러리 도메인 서비스 인터페이스
 *
 * @description
 * - 비디오 갤러리 문서, 카테고리, 번역, 첨부파일, 언어에 대한 통합 CRUD 명세를 정의합니다.
 * - 백엔드 API와의 계약을 추상화하여 Mock 서비스와 실 API 서비스의 교체를 용이하게 합니다.
 */
export interface VideoGalleryService {
  // ========== 비디오 갤러리 문서 CRUD 메서드 ==========
  /**
   * 비디오 갤러리 목록 조회
   *
   * @param code - CMS 문서 타입 코드 (필수)
   *   - 해당 code로 필터링하여 비디오 갤러리 목록을 조회합니다.
   *   - 카테고리 정보가 포함된 DTO를 반환합니다.
   */
  비디오_갤러리_목록을_조회_한다(
    code: DocumentCode,
  ): Promise<ApiResponse<VideoGalleryDto[]>>;

  /**
   * 비디오 갤러리 상세 조회
   *
   * @param documentId - 조회할 비디오 갤러리 ID
   *   - 카테고리 및 번역 정보가 포함된 DTO를 반환합니다.
   */
  비디오_갤러리를_조회_한다(
    documentId: string,
  ): Promise<ApiResponse<VideoGalleryDto>>;

  /**
   * 비디오 갤러리 생성
   *
   * @param data - 생성할 비디오 갤러리 데이터
   *   - categoryId를 포함할 수 있으며, 직접 외래키 관계로 카테고리를 연결합니다.
   */
  비디오_갤러리를_생성_한다(
    data: CreateVideoGalleryDto,
  ): Promise<ApiResponse<VideoGalleryDto>>;

  /**
   * 비디오 갤러리 수정
   *
   * @param documentId - 수정할 비디오 갤러리 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   *   - categoryId를 포함할 수 있으며, null로 설정 시 카테고리 관계를 제거합니다.
   */
  비디오_갤러리를_수정_한다(
    documentId: string,
    data: UpdateVideoGalleryDto,
  ): Promise<ApiResponse<VideoGalleryDto>>;

  /**
   * 비디오 갤러리 삭제
   *
   * 비디오 갤러리를 삭제하면 메타데이터와 관련된 모든 언어별 비디오 갤러리도 함께 삭제됩니다.
   *
   * @param documentId - 삭제할 비디오 갤러리 ID
   */
  비디오_갤러리를_삭제_한다(documentId: string): Promise<ApiResponse<void>>;

  // ========== 카테고리 CRUD 메서드 ==========
  /**
   * 카테고리 목록 조회
   *
   * @param code - CMS 문서 타입 코드 (필수)
   *   - 해당 code로 필터링하여 카테고리 목록을 조회합니다.
   */
  카테고리_목록을_조회_한다(
    code: DocumentCode,
  ): Promise<ApiResponse<VideoGalleryCategoryDto[]>>;

  /**
   * 카테고리 생성
   *
   * @param data - 생성할 카테고리 데이터
   */
  카테고리를_생성_한다(
    data: CreateVideoGalleryCategoryDto,
  ): Promise<ApiResponse<VideoGalleryCategoryDto>>;

  /**
   * 카테고리 수정
   *
   * @param categoryId - 수정할 카테고리 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  카테고리를_수정_한다(
    categoryId: string,
    data: UpdateVideoGalleryCategoryDto,
  ): Promise<ApiResponse<VideoGalleryCategoryDto>>;

  /**
   * 카테고리 삭제
   *
   * @param categoryId - 삭제할 카테고리 ID
   */
  카테고리를_삭제_한다(categoryId: string): Promise<ApiResponse<void>>;

  // ========== 번역 CRUD 메서드 ==========
  /**
   * 번역 목록 조회
   *
   * 특정 문서의 모든 번역을 조회합니다.
   *
   * @param documentId - 문서 ID
   */
  번역_목록을_조회_한다(
    documentId: string,
  ): Promise<ApiResponse<VideoGalleryTranslationDto[]>>;

  /**
   * 번역 조회
   *
   * 특정 문서의 특정 언어 번역을 조회합니다.
   * 언어 및 첨부파일 정보가 포함된 DTO를 반환합니다.
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  번역을_조회_한다(
    documentId: string,
    languageId: string,
  ): Promise<ApiResponse<VideoGalleryTranslationDto>>;

  /**
   * 번역 생성
   *
   * @param data - 생성할 번역 데이터
   */
  번역을_생성_한다(
    data: CreateVideoGalleryTranslationDto,
  ): Promise<ApiResponse<VideoGalleryTranslationDto>>;

  /**
   * 번역 수정
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  번역을_수정_한다(
    documentId: string,
    languageId: string,
    data: UpdateVideoGalleryTranslationDto,
  ): Promise<ApiResponse<VideoGalleryTranslationDto>>;

  /**
   * 번역 삭제
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  번역을_삭제_한다(
    documentId: string,
    languageId: string,
  ): Promise<ApiResponse<void>>;

  // ========== 첨부파일 CRUD 메서드 ==========

  /**
   * 첨부파일 목록 조회
   *
   * 특정 번역의 모든 첨부파일을 조회합니다.
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  첨부파일_목록을_조회_한다(
    documentId: string,
    languageId: string,
  ): Promise<ApiResponse<VideoGalleryAttachmentDto[]>>;

  /**
   * 첨부파일 조회
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   */
  첨부파일을_조회_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
  ): Promise<ApiResponse<VideoGalleryAttachmentDto>>;

  /**
   * 첨부파일 생성
   *
   * @param data - 생성할 첨부파일 데이터
   */
  첨부파일을_생성_한다(
    data: CreateVideoGalleryAttachmentDto,
  ): Promise<ApiResponse<VideoGalleryAttachmentDto>>;

  /**
   * 첨부파일 수정
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  첨부파일을_수정_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
    data: UpdateVideoGalleryAttachmentDto,
  ): Promise<ApiResponse<VideoGalleryAttachmentDto>>;

  /**
   * 첨부파일 삭제
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   */
  첨부파일을_삭제_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
  ): Promise<ApiResponse<void>>;

  // ========== 언어 CRUD 메서드 ==========
  /**
   * 언어 목록 조회
   */
  언어_목록을_조회_한다(): Promise<ApiResponse<HomepageLanguageEntity[]>>;

  /**
   * 언어 생성
   *
   * @param data - 생성할 언어 데이터
   */
  언어를_생성_한다(
    data: CreateHomepageLanguageDto,
  ): Promise<ApiResponse<HomepageLanguageEntity>>;

  /**
   * 언어 수정
   *
   * @param languageId - 수정할 언어 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  언어를_수정_한다(
    languageId: string,
    data: UpdateHomepageLanguageDto,
  ): Promise<ApiResponse<HomepageLanguageEntity>>;

  /**
   * 언어 삭제
   *
   * @param languageId - 삭제할 언어 ID
   */
  언어를_삭제_한다(languageId: string): Promise<ApiResponse<void>>;
}
```

뉴스

```typescript
import { ApiResponse } from '@/app/(planning)/plan/cms/_types/common.model';
import { DocumentCode } from '@/cms/admin/homepage/_entities/homepage.entity';
import {
  NewsDto,
  NewsCategoryDto,
  NewsTranslationDto,
  NewsAttachmentDto,
  NewsUrlDto,
  CreateNewsDto,
  UpdateNewsDto,
  CreateNewsCategoryDto,
  UpdateNewsCategoryDto,
  CreateNewsTranslationDto,
  UpdateNewsTranslationDto,
  CreateNewsAttachmentDto,
  UpdateNewsAttachmentDto,
  CreateNewsUrlDto,
  UpdateNewsUrlDto,
} from '../_entities/news.dto';
import { HomepageLanguageEntity } from '@/cms/admin/homepage/_entities/homepage.entity';
import {
  CreateHomepageLanguageDto,
  UpdateHomepageLanguageDto,
} from '@/cms/admin/homepage/_entities/homepage.dto';

/**
 * 뉴스 도메인 서비스 인터페이스
 *
 * @description
 * - 뉴스 문서, 카테고리, 번역, 첨부파일, 언어에 대한 통합 CRUD 명세를 정의합니다.
 * - 백엔드 API와의 계약을 추상화하여 Mock 서비스와 실 API 서비스의 교체를 용이하게 합니다.
 */
export interface NewsService {
  // ========== 뉴스 문서 CRUD 메서드 ==========
  /**
   * 뉴스 목록 조회
   *
   * @param code - CMS 문서 타입 코드 (필수)
   *   - 해당 code로 필터링하여 뉴스 목록을 조회합니다.
   *   - 카테고리 정보가 포함된 DTO를 반환합니다.
   */
  뉴스_목록을_조회_한다(code: DocumentCode): Promise<ApiResponse<NewsDto[]>>;

  /**
   * 뉴스 상세 조회
   *
   * @param documentId - 조회할 뉴스 ID
   *   - 카테고리 및 번역 정보가 포함된 DTO를 반환합니다.
   */
  뉴스를_조회_한다(documentId: string): Promise<ApiResponse<NewsDto>>;

  /**
   * 뉴스 생성
   *
   * @param data - 생성할 뉴스 데이터
   *   - categoryId를 포함할 수 있으며, 직접 외래키 관계로 카테고리를 연결합니다.
   */
  뉴스를_생성_한다(data: CreateNewsDto): Promise<ApiResponse<NewsDto>>;

  /**
   * 뉴스 수정
   *
   * @param documentId - 수정할 뉴스 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   *   - categoryId를 포함할 수 있으며, null로 설정 시 카테고리 관계를 제거합니다.
   */
  뉴스를_수정_한다(
    documentId: string,
    data: UpdateNewsDto,
  ): Promise<ApiResponse<NewsDto>>;

  /**
   * 뉴스 삭제
   *
   * 뉴스를 삭제하면 메타데이터와 관련된 모든 언어별 뉴스도 함께 삭제됩니다.
   *
   * @param documentId - 삭제할 뉴스 ID
   */
  뉴스를_삭제_한다(documentId: string): Promise<ApiResponse<void>>;

  // ========== 카테고리 CRUD 메서드 ==========
  /**
   * 카테고리 목록 조회
   *
   * @param code - CMS 문서 타입 코드 (필수)
   *   - 해당 code로 필터링하여 카테고리 목록을 조회합니다.
   */
  카테고리_목록을_조회_한다(
    code: DocumentCode,
  ): Promise<ApiResponse<NewsCategoryDto[]>>;

  /**
   * 카테고리 생성
   *
   * @param data - 생성할 카테고리 데이터
   */
  카테고리를_생성_한다(
    data: CreateNewsCategoryDto,
  ): Promise<ApiResponse<NewsCategoryDto>>;

  /**
   * 카테고리 수정
   *
   * @param categoryId - 수정할 카테고리 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  카테고리를_수정_한다(
    categoryId: string,
    data: UpdateNewsCategoryDto,
  ): Promise<ApiResponse<NewsCategoryDto>>;

  /**
   * 카테고리 삭제
   *
   * @param categoryId - 삭제할 카테고리 ID
   */
  카테고리를_삭제_한다(categoryId: string): Promise<ApiResponse<void>>;

  // ========== 번역 CRUD 메서드 ==========
  /**
   * 번역 목록 조회
   *
   * 특정 문서의 모든 번역을 조회합니다.
   *
   * @param documentId - 문서 ID
   */
  번역_목록을_조회_한다(
    documentId: string,
  ): Promise<ApiResponse<NewsTranslationDto[]>>;

  /**
   * 번역 조회
   *
   * 특정 문서의 특정 언어 번역을 조회합니다.
   * 언어 및 첨부파일 정보가 포함된 DTO를 반환합니다.
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  번역을_조회_한다(
    documentId: string,
    languageId: string,
  ): Promise<ApiResponse<NewsTranslationDto>>;

  /**
   * 번역 생성
   *
   * @param data - 생성할 번역 데이터
   */
  번역을_생성_한다(
    data: CreateNewsTranslationDto,
  ): Promise<ApiResponse<NewsTranslationDto>>;

  /**
   * 번역 수정
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  번역을_수정_한다(
    documentId: string,
    languageId: string,
    data: UpdateNewsTranslationDto,
  ): Promise<ApiResponse<NewsTranslationDto>>;

  /**
   * 번역 삭제
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  번역을_삭제_한다(
    documentId: string,
    languageId: string,
  ): Promise<ApiResponse<void>>;

  // ========== 첨부파일 CRUD 메서드 ==========

  /**
   * 첨부파일 목록 조회
   *
   * 특정 번역의 모든 첨부파일을 조회합니다.
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   */
  첨부파일_목록을_조회_한다(
    documentId: string,
    languageId: string,
  ): Promise<ApiResponse<NewsAttachmentDto[]>>;

  /**
   * 첨부파일 조회
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   */
  첨부파일을_조회_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
  ): Promise<ApiResponse<NewsAttachmentDto>>;

  /**
   * 첨부파일 생성
   *
   * @param data - 생성할 첨부파일 데이터
   */
  첨부파일을_생성_한다(
    data: CreateNewsAttachmentDto,
  ): Promise<ApiResponse<NewsAttachmentDto>>;

  /**
   * 첨부파일 수정
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  첨부파일을_수정_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
    data: UpdateNewsAttachmentDto,
  ): Promise<ApiResponse<NewsAttachmentDto>>;

  /**
   * 첨부파일 삭제
   *
   * @param documentId - 문서 ID
   * @param languageId - 언어 ID
   * @param attachmentId - 첨부파일 ID
   */
  첨부파일을_삭제_한다(
    documentId: string,
    languageId: string,
    attachmentId: string,
  ): Promise<ApiResponse<void>>;

  // ========== 언어 CRUD 메서드 ==========
  /**
   * 언어 목록 조회
   */
  언어_목록을_조회_한다(): Promise<ApiResponse<HomepageLanguageEntity[]>>;

  /**
   * 언어 생성
   *
   * @param data - 생성할 언어 데이터
   */
  언어를_생성_한다(
    data: CreateHomepageLanguageDto,
  ): Promise<ApiResponse<HomepageLanguageEntity>>;

  /**
   * 언어 수정
   *
   * @param languageId - 수정할 언어 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  언어를_수정_한다(
    languageId: string,
    data: UpdateHomepageLanguageDto,
  ): Promise<ApiResponse<HomepageLanguageEntity>>;

  /**
   * 언어 삭제
   *
   * @param languageId - 삭제할 언어 ID
   */
  언어를_삭제_한다(languageId: string): Promise<ApiResponse<void>>;

  // ========== URL CRUD 메서드 ==========
  /**
   * URL 조회
   *
   * 특정 문서의 URL을 조회합니다.
   *
   * @param documentId - 문서 ID
   */
  URL을_조회_한다(documentId: string): Promise<ApiResponse<NewsUrlDto>>;

  /**
   * URL 생성
   *
   * @param data - 생성할 URL 데이터
   */
  URL을_생성_한다(data: CreateNewsUrlDto): Promise<ApiResponse<NewsUrlDto>>;

  /**
   * URL 수정
   *
   * @param documentId - 문서 ID
   * @param data - 수정할 데이터 (부분 업데이트 가능)
   */
  URL을_수정_한다(
    documentId: string,
    data: UpdateNewsUrlDto,
  ): Promise<ApiResponse<NewsUrlDto>>;

  /**
   * URL 삭제
   *
   * @param documentId - 문서 ID
   */
  URL을_삭제_한다(documentId: string): Promise<ApiResponse<void>>;
}
```

공지사항

```typescript
import type { ApiResponse } from '@/cms/_types/common.model';
import type { CmsService } from '@/cms/_service/cms.interface';
import type {
  AnnouncementDto,
  AnnouncementCategoryDto,
  AnnouncementAttachmentDto,
  AnnouncementTargetDto,
  AnnouncementRespondedDto,
  CreateAnnouncementDto,
  UpdateAnnouncementDto,
  CreateAnnouncementCategoryDto,
  UpdateAnnouncementCategoryDto,
} from '../_entities/announcement.dto';

// ApiResponse 재export
export type { ApiResponse };

/**
 * 공지사항 도메인 서비스 인터페이스
 *
 * @description
 * - 공지사항 문서, 카테고리, 응답 상태에 대한 통합 CRUD 명세를 정의합니다.
 * - 백엔드 API와의 계약을 추상화하여 Mock 서비스와 실 API 서비스의 교체를 용이하게 합니다.
 */
export interface AnnouncementService extends Pick<
  CmsService,
  '알림을_전송_한다'
> {
  // ========== 공지사항 CRUD 메서드 ==========
  /**
   * 공지사항 목록 조회
   */
  공지사항_목록을_조회_한다: () => Promise<ApiResponse<AnnouncementDto[]>>;

  /**
   * 공지사항 상세 조회
   */
  공지사항을_조회_한다: (
    documentId: string,
  ) => Promise<ApiResponse<AnnouncementDto>>;

  /**
   * 공지사항 생성
   */
  공지사항을_생성_한다: (
    data: CreateAnnouncementDto,
  ) => Promise<ApiResponse<AnnouncementDto>>;

  /**
   * 공지사항 수정
   */
  공지사항을_수정_한다: (
    documentId: string,
    data: UpdateAnnouncementDto,
  ) => Promise<ApiResponse<AnnouncementDto>>;

  /**
   * 공지사항 삭제
   */
  공지사항을_삭제_한다: (documentId: string) => Promise<ApiResponse<void>>;

  // ========== 카테고리 CRUD 메서드 ==========
  /**
   * 카테고리 목록 조회
   */
  카테고리_목록을_조회_한다: () => Promise<
    ApiResponse<AnnouncementCategoryDto[]>
  >;

  /**
   * 카테고리 생성
   */
  카테고리를_생성_한다: (
    data: CreateAnnouncementCategoryDto,
  ) => Promise<ApiResponse<AnnouncementCategoryDto>>;

  /**
   * 카테고리 수정
   */
  카테고리를_수정_한다: (
    categoryId: string,
    data: UpdateAnnouncementCategoryDto,
  ) => Promise<ApiResponse<AnnouncementCategoryDto>>;

  /**
   * 카테고리 삭제
   */
  카테고리를_삭제_한다: (categoryId: string) => Promise<ApiResponse<void>>;

  // ========== 기타 조회 메서드 (상세 화면용) ==========
  /**
   * 특정 공지사항의 모든 첨부파일을 조회합니다.
   */
  첨부파일_목록을_조회_한다: (
    documentId: string,
  ) => Promise<ApiResponse<AnnouncementAttachmentDto[]>>;

  /**
   * 특정 공지사항의 모든 대상자를 조회합니다.
   */
  대상자_목록을_조회_한다: (
    documentId: string,
  ) => Promise<ApiResponse<AnnouncementTargetDto[]>>;

  /**
   * 특정 공지사항의 모든 응답 상태를 조회합니다.
   */
  응답_상태_목록을_조회_한다: (
    documentId: string,
  ) => Promise<ApiResponse<AnnouncementRespondedDto[]>>;
}
```
